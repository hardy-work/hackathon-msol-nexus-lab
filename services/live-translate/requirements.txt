# Base deps (all backends). Backend-specific extras are installed by deploy.sh
# depending on TRANSLATE_BACKEND / available Python:
#   subscription -> claude-agent-sdk  (needs Python >= 3.10)
#   api          -> anthropic
#   cli          -> none (shells out to the `claude` CLI; works on Python 3.9)
fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27
