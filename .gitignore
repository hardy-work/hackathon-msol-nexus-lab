# Secrets / machine-specific config — never commit
.mcp.json
.claude/settings.local.json

# Python
vexa-mcp/.venv/
vexa-mcp/.env

# Skill secrets — never commit real .env files, only .env.example
openclaw-skills/*/.env
openclaw-skills/*/service-account*.json
**/__pycache__/

.DS_Store
