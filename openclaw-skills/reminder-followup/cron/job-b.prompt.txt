Việc B skill reminder-followup (follow-up). Phiên cron KHÔNG có tool slack — dùng tool Bash gọi CLI `openclaw message` để đọc/gửi Slack. Làm tuần tự, không delegate cho subagent. Kết thúc PHẢI trả về đúng 1 dòng trạng thái theo mẫu ở cuối.

BOT_USER_ID = U0BKL0DJV7B (chính bot này, không bao giờ tính vào danh sách phải report).
CHANNEL = C0BKLP5KYD7

1. Nếu giờ hiện tại (Asia/Ho_Chi_Minh) đã qua 17:30 → trả về "SKIP | qua gio cutoff".

2. Tìm thread. Chạy:
   openclaw message read --channel slack --target C0BKLP5KYD7 --limit 60 --json
   Trong payload.messages, lọc các tin có user == U0BKL0DJV7B VÀ text chứa "Đến giờ report task rồi". Với mỗi tin ứng viên, KIỂM TRA NGÀY BẰNG LỆNH, TUYỆT ĐỐI KHÔNG NHẨM TRONG ĐẦU:
     TODAY=$(TZ=Asia/Ho_Chi_Minh date +%F)
     MSGDAY=$(TZ=Asia/Ho_Chi_Minh date -d @<ts> +%F)
   Chỉ nhận tin có MSGDAY == TODAY (so sánh chuỗi y hệt nhau). Tin của ngày hôm trước PHẢI bị loại — nhắc vào thread cũ là lỗi nghiêm trọng, thà không nhắc còn hơn.
   Trong các tin còn lại, lấy tin MỚI NHẤT; THREAD_TS = ts của tin đó. Không còn tin nào → trả về "SKIP | khong tim thay thread hom nay", TUYỆT ĐỐI không đăng tin mới và không đụng vào thread của ngày cũ.

3. Đọc reply:
   openclaw message read --channel slack --target C0BKLP5KYD7 --thread-id <THREAD_TS> --json
   Bỏ qua chính tin gốc và mọi tin có user == U0BKL0DJV7B. Với mỗi user còn lại, xét từng DÒNG trong text của họ.

   BƯỚC 3.1 — ĐỌC LUẬT CHẤM. TRƯỚC KHI CHẤM BẤT KỲ DÒNG NÀO, chạy:
   cat /home/sonbh/.openclaw/workspace/skills/reminder-followup/template/log-task-rules.md
   File này là NGUỒN DUY NHẤT của luật format (dọn dòng, chọn dòng log, 7 điều kiện hợp lệ, thế nào là Status Done, chấm cả cụm). TUYỆT ĐỐI KHÔNG chấm bằng trí nhớ, KHÔNG tái tạo luật từ tin nhắn cũ trong thread, KHÔNG suy ra từ dòng VD trong tin nhắc. Lệnh cat lỗi / file không tồn tại → DỪNG, không chấm ai, trả về "ERROR | khong doc duoc log-task-rules.md".

   BƯỚC 3.2 — CHẤM theo đúng file vừa đọc, không thêm bớt điều kiện nào. Nhắc lại 3 chỗ hay sai nhất: (1) TUYỆT ĐỐI không nhắc ai về dạng mã task — "4", "abc 12", "DWM-2222" hợp lệ như "NEX-214"; (2) Status chưa Done thì End date để trống hoặc "-"/"N/A"/"chưa" đều HỢP LỆ; (3) không chắc một dòng có hợp lệ hay không → coi là HỢP LỆ.

   BƯỚC 3.3 — KẾT LUẬN CHO TỪNG NGƯỜI theo mục "Bước 4 — Kết luận cho từng người" trong file: ĐÃ REPORT = có ÍT NHẤT 1 dòng log VÀ TẤT CẢ dòng log đều hợp lệ. Chỉ cần MỘT dòng sai → SAI FORMAT. Có reply nhưng KHÔNG CÓ DÒNG LOG NÀO cũng là SAI FORMAT.

4. Xác định AI PHẢI REPORT bằng lệnh. NGUỒN DUY NHẤT là tab "Resource plan" của Google Sheet logtime (cột Member + Slack ID) — TUYỆT ĐỐI không dùng danh sách thành viên kênh, không có file roster nào nữa:
   bash /home/sonbh/.openclaw/workspace/skills/reminder-followup/scripts/resource-plan-members.sh
   Lệnh in ra JSON: {"date","found_day_column","people":[{id,name,slack_name,role}],"off":[...],"no_id":[...],"notices":["..."]}
   (a0) EXIT 6 = hôm nay CẢ ĐỘI NGHỈ (T7/CN hoặc cả đội off). KHÔNG reply gì cả, KHÔNG cảnh báo ai — trả về "SKIP | ca doi nghi hom nay". Đây KHÔNG phải lỗi, tuyệt đối không đăng cảnh báo vào ngày nghỉ.
   (a) EXIT 2/3/4/5 = không đọc được danh sách. TUYỆT ĐỐI không nhắc ai, không đoán danh sách, KHÔNG dùng <!here>/<!channel>, và KHÔNG được trả về "tat ca da report" (không đọc được khác hẳn với mọi người đã report). Thay vào đó: xem lại TOÀN BỘ reply thô của thread ở bước 3, KỂ CẢ tin của U0BKL0DJV7B. Đã có tin nào của bot chứa chuỗi "chưa đọc được danh sách người phải report" → trả về "SKIP | khong doc duoc sheet, da bao roi" và KHÔNG đăng thêm gì (2 lượt cron không được báo 2 lần). Chưa báo → gửi đúng 1 reply vào thread, KHÔNG tag ai, KHÔNG đăng tin mới ra kênh:
       openclaw message send --channel slack --target C0BKLP5KYD7 --reply-to <THREAD_TS> --json --message "<nội dung>"
       Nội dung đúng nguyên văn:

⚠️ Mình chưa đọc được danh sách người phải report từ Google Sheet logtime (tab Resource plan) nên chưa nhắc được ai. Nhờ PM kiểm tra lại link/quyền chia sẻ của sheet giúp mình nhé.

       Gửi xong trả về "NOTIFIED | khong doc duoc sheet (exit <mã>)". Gửi lỗi → "ERROR | khong gui duoc canh bao sheet".
   (b) EXIT 0 → PHAI_REPORT = mảng "people" (bỏ U0BKL0DJV7B nếu có mặt). TUYỆT ĐỐI KHÔNG tag bất kỳ ai trong mảng "off": họ NGHỈ hôm nay, KHÔNG tính vào chưa report, KHÔNG hỏi han gì họ cả — im hẳn. Không cần kiểm họ có trong group chat hay không.
   (c) Chia 2 nhóm TRONG PHAI_REPORT: CHUA_REPORT = id không hề reply trong thread; SAI_FORMAT = id có reply nhưng theo BƯỚC 3.3 không đạt ĐÃ REPORT (không có dòng log nào, hoặc có >=1 dòng log sai). Cả 2 rỗng VÀ nhóm (e) cũng rỗng → trả về "SKIP | tat ca da report".
   (d) Mảng "notices" khác rỗng → dán y nguyên từng phần tử vào CUỐI tin ở bước 5, mỗi phần tử 1 dòng. Đây là các dòng phụ DUY NHẤT được thêm vào tin.
   (e) NHÓM QUA_HAN_GIAI_TRINH — task bị treo vì vượt giờ plan mà dev không giải trình. Chạy:
       NOW=$(date +%s); cat /home/sonbh/.openclaw/workspace/skills/reminder-followup/state/pending-overtime.json 2>/dev/null
       File không tồn tại / rỗng / JSON hỏng → coi như nhóm này RỖNG, đi tiếp bình thường, KHÔNG báo lỗi (chưa ai vượt giờ hôm nay là chuyện thường).
       Lấy các phần tử có (NOW - asked_at) > 3600. TÍNH BẰNG LỆNH, KHÔNG NHẨM TRONG ĐẦU. Gom theo slack_id: mỗi người 1 mention + danh sách task_id của họ cách nhau dấu phẩy.
       Người trong nhóm này KHÔNG bị loại khỏi 2 nhóm kia và cũng không thay thế 2 nhóm kia — 3 nhóm độc lập, một người có thể vừa sai format vừa quá hạn giải trình.

5. Gửi đúng 1 reply vào thread, KHÔNG đăng tin mới ra kênh, KHÔNG DM ai:
   openclaw message send --channel slack --target C0BKLP5KYD7 --reply-to <THREAD_TS> --json --message "<nội dung>"
   Nội dung đúng nguyên văn, mỗi <@Uxxxx> cách nhau 1 dấu cách, id lấy từ JSON ở bước 4 (KHÔNG gõ tên người thay cho mention):

<mention_chua_report> chưa report hôm nay nhé!
<mention_sai_format> đã report nhưng chưa đúng mẫu, sửa lại giúp mình nhé!
<mention_qua_han_giai_trinh> chưa nói lý do vượt giờ plan nên các task <ids> mình chưa log lên sheet nhé!

Report theo mẫu sau :
Id task | Re-estimate (h) | Start date | End date | Actual Effort (h) | Status | Note

VD: NEX-214 | 8 | 03-08-2026 | 04-08-2026 | 7.5 | Done | xong sớm nửa buổi
<các dòng trong "notices" nếu có>

   NHÓM NÀO RỖNG THÌ BỎ HẲN DÒNG ĐÓ. Luôn giữ phần "Report theo mẫu sau :". Không trích lại nội dung sai của ai, chỉ tag và trỏ về mẫu — NGOẠI LỆ DUY NHẤT là <ids> của dòng quá hạn giải trình (chỉ id task, KHÔNG nêu số giờ, KHÔNG nêu lý do, KHÔNG bình luận). Lệnh gửi lỗi → trả về "ERROR | <mô tả ngắn>", không thử lại nhiều lần.

6. Gửi xong, XOÁ các phần tử quá hạn ở bước 4(e) khỏi /home/sonbh/.openclaw/workspace/skills/reminder-followup/state/pending-overtime.json (giữ nguyên các phần tử còn hạn), ghi lại file bằng lệnh (python3 đọc-lọc-ghi, đừng sửa JSON bằng sed). Mỗi task chỉ được nhắc đúng 1 lần, không lôi sang ngày hôm sau. Bước 5 gửi lỗi thì ĐỪNG xoá.

DÒNG TRẢ VỀ CUỐI CÙNG (chỉ 1 dòng, không thêm chữ nào khác):
- REPLIED | chua report: <N> | sai format: <M> | qua han giai trinh: <K>
- NOTIFIED | khong doc duoc sheet (exit <mã>)
- SKIP | <lý do: qua gio cutoff / khong tim thay thread hom nay / tat ca da report / ca doi nghi hom nay / khong doc duoc sheet, da bao roi>
- ERROR | <mô tả ngắn>
