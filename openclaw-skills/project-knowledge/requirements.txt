openpyxl==3.1.5      # đọc .xlsx ở Stage 2 (trích xuất khai báo)
PyYAML==6.0.2        # đọc extract/*.yml, coverage.yml, frontmatter
duckdb==1.1.3        # derived/facts.duckdb — tầng số cho bậc 1
python-frontmatter==1.1.0  # parse frontmatter của wiki/*.md
python-docx==1.2.0   # luồng VĂN (v1.5) — trích .docx
PyMuPDF==1.28.0      # luồng VĂN (v1.5) — trích .pdf (text) + phát hiện PDF scan
# OCR (nhánh SCAN) là phần mềm HỆ THỐNG, không cài bằng pip:
#   ocrmypdf + tesseract (-l vie+eng) — cần cho PDF ảnh như noi-quy-lao-dong.pdf
