fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27
# Default subscription path: warm, persistent `claude` session via the Agent SDK.
claude-agent-sdk>=0.2
# Only needed when TRANSLATE_BACKEND=api:
anthropic>=0.40
