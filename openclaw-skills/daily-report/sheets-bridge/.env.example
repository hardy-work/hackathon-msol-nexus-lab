# Path to the Google Cloud service account JSON key file (Sheets API enabled,
# and the target spreadsheet shared with the service account's email).
GOOGLE_SHEETS_CREDENTIALS_JSON=/absolute/path/to/service-account.json

# Target spreadsheet id — from its URL: .../spreadsheets/d/<THIS_PART>/edit
GOOGLE_SHEETS_SPREADSHEET_ID=
